/// Web/stub: кадр из видео недоступен.
abstract final class GalleryVideoThumbnail {
  static Future<String?> ensureForAttachment(
    Map<String, dynamic> attachment, {
    int maxWidth = 512,
    int timeMs = 500,
  }) async =>
      null;
}
